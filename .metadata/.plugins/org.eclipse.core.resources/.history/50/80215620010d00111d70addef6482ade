/**
 ******************************************************************************
 * @file           : main.c
 * @author         : Auto-generated by STM32CubeIDE
 * @brief          : Main program body
 ******************************************************************************
 * @attention
 *
 * Copyright (c) 2026 STMicroelectronics.
 * All rights reserved.
 *
 * This software is licensed under terms that can be found in the LICENSE file
 * in the root directory of this software component.
 * If no LICENSE file comes with this software, it is provided AS-IS.
 *
 ******************************************************************************
 */

#include <stdint.h>
#include <stdio.h>
#if !defined(__SOFT_FP__) && defined(__ARM_FP)
  #warning "FPU is not initialized, but the project is compiling for an FPU. Please initialize the FPU before use."
#endif
uint32_t Addition(uint32_t x, uint32_t y)
{
	__asm ("SVC #35");
	uint32_t data;
	__asm volatile ("MOV %0, R0": "=r"(data));
	return data;
}
uint32_t Subtraction(uint32_t x, uint32_t y)
{
	__asm ("SVC #36");
	uint32_t data;
	__asm volatile ("MOV %0, R0": "=r"(data));
	return data;
}uint32_t Division(uint32_t x, uint32_t y)
{
	__asm ("SVC #37");
	uint32_t data;
	__asm volatile ("MOV %0, R0": "=r"(data));
	return data;
}uint32_t Multiply(uint32_t x, uint32_t y)
{
	__asm ("SVC #38");
	uint32_t data;
	__asm volatile ("MOV %0, R0": "=r"(data));
	return data;
}
int main(void)
{
    /* Loop forever */

	printf("In the SVC Opcode Fetch exercise \n");
	//__asm ("SVC #25");
	uint32_t result1= Addition (10,20);
	uint32_t result2 = Subtraction (10,20);
	uint32_t result3 = Division (10,20);
	uint32_t result4 = Multiply (10,20);


	for(;;);
}
__attribute ((naked))void SVC_Handler(void)
{
	__asm ("MRS R0,MSP");
	__asm ("B SVC_Handler_call");
}
void SVC_Handler_call(uint32_t* MainStackPtr )
{
	printf("In SVC Handler call \n");
	uint8_t * numberFetch = (uint8_t*)MainStackPtr[6];
    numberFetch -= 2;

    uint32_t arg0 = MainStackPtr[0];

    uint32_t arg1 = MainStackPtr[1];

	//printf ("%lx , %lx , %lx \n", MainStackPtr, &MainStackPtr[6], numberFetch);
    printf("%d \n", *numberFetch);
   // printf("%lx \n", MainStackPtr[6]);
    uint8_t SVC_number = *numberFetch;
    uint32_t result;
    switch(SVC_number)
    {
    case 35:
    	result = arg0 + arg1;
    	break;

    case 36:
    	result = arg0 - arg1;
    	break;
    case 37:
    	result = arg0 / arg1;
    	break;
    case 38:
    	result = arg0 * arg1;
    	break;
    default:
    	printf("false case \n");

    }

    MainStackPtr[0] = result;


}
